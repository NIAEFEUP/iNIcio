import { db } from "@/lib/db";
import {
  user,
  candidate,
  recruiterToCandidate,
  recruiter,
  account,
  application,
  recruitmentPhase,
  recruitment,
  slot,
  interview,
  dynamic,
} from "./schema";

async function main() {
  // await db.delete(interview);
}

main();
